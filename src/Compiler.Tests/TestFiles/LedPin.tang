import Pin

class LedPin
    Pin ledPin
    nothing init(register8 ledDdr, register8 ledPort, uint8 ledBitIndex)
        ledPin.init(ledDdr, ledPort, ledBitIndex)
    nothing on()
        ledPin.on()
    nothing off()
        ledPin.off()