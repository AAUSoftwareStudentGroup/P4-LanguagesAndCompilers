import Nano as nano

import Nano as nano2

uint8 button
uint8 led

button = 10
led = 13
bool value
value = false

nothing func()
    value = !value
    nano.DigitalWrite(led, value)

interrupt(2) // INT0_vect
    func()

DigitalWrite(2, HIGH)

INT0_init(FALLING_EDGE)

DigitalWrite(led, LOW)